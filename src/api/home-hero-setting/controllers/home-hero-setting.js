'use strict';

/**
 * home-hero-setting controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::home-hero-setting.home-hero-setting');
