'use strict';

/**
 * home-hero-setting service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::home-hero-setting.home-hero-setting');
