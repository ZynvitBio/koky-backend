'use strict';

/**
 * same-day-product controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::same-day-product.same-day-product');
